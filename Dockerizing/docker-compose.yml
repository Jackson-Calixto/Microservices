version: '3.4'

services:
  naming-server:
    image: jacksoncalixto/naming-server:0.0.1-SNAPSHOT
    ports:
      - 8761:8761
    networks:
      - calixto-network
networks:
  calixto-network:
    driver: bridge